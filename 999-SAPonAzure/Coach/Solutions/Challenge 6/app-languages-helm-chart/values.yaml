# Default values for app-languages.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

appData:
  labelValue: "langfacts"
  imageName: "whatthehackmsft/langfacts"
  imageVersion: "v1"
  namespace: "whatthehack"

serviceDefinition:
  name: "langfactsservice"
  type: "LoadBalancer"
   
