# Default values for app-languages.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

appData:
  labelValue: "nginx-webapp"
  imageName: "izzyacademy/nginxserver"
  imageVersion: "latest"
  namespace: "p20-webapps"

serviceDefinition:
  name: "webapp-loadbalancer"
  type: "LoadBalancer"
   
